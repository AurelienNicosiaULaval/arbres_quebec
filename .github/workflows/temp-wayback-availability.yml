name: Récupération des PDF par l'API de disponibilité Wayback
on:
  pull_request:
    branches: [main]
  workflow_dispatch:
jobs:
  download:
    runs-on: ubuntu-latest
    timeout-minutes: 25
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: Installer les dépendances
        run: python -m pip install --upgrade requests pypdf
      - name: Récupérer les meilleures captures disponibles
        run: |
          set +e
          python temp_wayback_availability_recovery.py > run_wayback_availability.log 2>&1
          status=$?
          cat run_wayback_availability.log
          exit $status
      - name: Créer l'archive
        if: always()
        run: |
          python - <<'PY'
          import shutil
          shutil.make_archive("notes-master-math-france-wayback-availability", "zip", "notes_master_math_france_wayback_availability")
          PY
      - name: Publier l'archive
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: notes-master-math-france-wayback-availability
          path: |
            notes-master-math-france-wayback-availability.zip
            notes_master_math_france_wayback_availability/manifest.csv
            notes_master_math_france_wayback_availability/manifest.json
            run_wayback_availability.log
          if-no-files-found: error
          retention-days: 3
